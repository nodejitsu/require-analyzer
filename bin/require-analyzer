#!/usr/bin/env node

require.paths.unshift(require('path').join(__dirname, '..', 'lib'));

var analyzer = require('require-analyzer');

analyzer.package(process.cwd(), function (err, packages) {
  require('eyes').inspect(packages);
});